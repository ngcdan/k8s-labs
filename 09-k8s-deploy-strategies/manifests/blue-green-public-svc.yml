apiVersion: v1
kind: Service
metadata:
  name: nginx-public
spec:
  type: LoadBalancer
  selector: { app: nginx, role: blue }     # ĐANG trỏ blue — đổi role: green để cutover tức thì
  ports: [{ port: 80, targetPort: 80 }]
