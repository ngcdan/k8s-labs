apiVersion: v1
kind: Service
metadata:
  name: myapp-svc
spec:
  type: LoadBalancer
  selector: { app: myapp }                 # CHỈ bắt label chung → gom CẢ stable lẫn canary vào 1 pool
  ports: [{ port: 80, targetPort: 80 }]
